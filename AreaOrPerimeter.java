public class AreaOrPerimeter {
    public static int areaOrPerimeter (int l, int w) {
        // code away...
      if (l == w)
        return l * w;
      else
        return (l * 2) + (w * 2);
      
    }
}